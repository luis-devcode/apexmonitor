{
  "name": "monitorodds-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "node --env-file-if-exists=.env src/collect.mjs",
    "collect:test": "node --env-file-if-exists=.env src/collect.mjs --once=12000",
    "probe": "node --env-file-if-exists=.env probe-schema.mjs",
    "probe:features": "node --env-file-if-exists=.env probe-features.mjs"
  }
}
